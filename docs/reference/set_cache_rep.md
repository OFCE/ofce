# répertoire de cache persistant

répertoire de cache persistant

## Usage

``` r
set_cache_rep(cache_rep = find_cache_rep())
```

## Arguments

- cache_rep:

  (character) le répertoire

## Value

rien

## See also

Other source_data: [`clear_source_cache`](clear_source_cache.md)`()`,
[`source_data`](source_data.md)`()`,
[`source_data_refresh`](source_data_refresh.md)`()`,
[`source_data_status`](source_data_status.md)`()`
