# Set RStudio to french dictionary

Pour écrire en français dans RStudio Attention un dictionnaire ne
garantit en rien la qualité de l'expression

## Usage

``` r
setup_french()
```

## Examples

``` r
if(interactive()) setup_french()
```
