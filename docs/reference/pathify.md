# déroule le chemin à partir du début du projet

Lorsque le chemin commence par un "/", il est calculé à partir de la
racine du projet

## Usage

``` r
pathify(path, root = NULL)
```

## Arguments

- path:

  chemin

- root:

  racine (absolu), NULL par défaut

## Value

chemin absolu (string)
