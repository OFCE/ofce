% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/get_icon_flags.R
\name{get_flags}
\alias{get_flags}
\title{Obtenir les icônes de drapeaux à partir de codes ou noms de pays}
\usage{
get_flags(country, tooltip = FALSE, size = 15)
}
\arguments{
\item{country}{Un vecteur de chaînes de caractères contenant des codes pays
ISO2 (\code{"FR"}), ISO3 (\code{"FRA"}) ou des noms de pays dans n'importe quelle
langue (\code{"France"}, \code{"Allemagne"}, \code{"Germany"}).
On peut mélanger les formats dans le même vecteur.
Les valeurs \code{NA} sont conservées dans le résultat.}

\item{tooltip}{Logique. Si \code{TRUE}, retourne une balise HTML \verb{<img>}.
Si \code{FALSE} (par défaut), retourne uniquement l'URL du SVG.
Passé à \code{\link[=get_icons]{get_icons()}}.}

\item{size}{Taille en pixels de l'icône lorsque \code{tooltip = TRUE}
(par défaut \code{15}). Passé à \code{\link[=get_icons]{get_icons()}}.}
}
\value{
Un vecteur de chaînes de caractères contenant les URLs ou balises
HTML des drapeaux Twemoji. Les éléments \code{NA} en entrée ou les pays
non reconnus produisent des \code{NA} en sortie.
}
\description{
Convertit un vecteur de codes pays ISO 3166-1 alpha-2, alpha-3 ou de
noms de pays (dans n'importe quelle langue) en URLs ou balises HTML
des drapeaux Twemoji correspondants, en s'appuyant sur \code{\link[=get_icons]{get_icons()}}.
La détection du format est automatique via le package \pkg{countrycode}.
}
\examples{
get_flags("FR")
get_flags(c("FRA", "DEU", "USA"))
get_flags(c("FR", "DEU", "US"))
get_flags("France")
get_flags(c("Allemagne", "Italy", "Estados Unidos"))
get_flags("FRA", tooltip = TRUE)

}
